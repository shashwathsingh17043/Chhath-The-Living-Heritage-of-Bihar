import type { Artisan } from "../types/heritage";

// Ethical decision: To avoid fabricating biography profiles (names, villages, interviews, etc.),
// this catalog is initialized as empty. The database remains ready to receive real, verified
// field-research details in subsequent iterations.
export const artisans: Artisan[] = [];
